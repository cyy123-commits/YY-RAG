历史会话：
{history_text}

当前用户问题：{query}

请根据历史会话和当前问题，提取用户正在询问的商品名称（item_names）。

提取规则：
1. 如果用户明确提到了商品名称，请提取出来。商品名称通常是品牌+型号+类别（如"HAK 180 烫金机"、"RS-12数字万用表"）。
2. 注意：用户可能在商品名称后紧跟一个逗号/空格和具体问题（如"HAK 180 烫金机，如何更换保险丝？"），此时只提取商品名称部分"HAK 180 烫金机"，不要把后面的问题文字包含进去。
3. 如果用户使用了代词（如"这个"、"它"），请结合历史会话指代消解，确定商品名称。
4. 如果无法确定商品名称，item_names 返回空列表。
5. 请重新改写用户的问题（rewritten_query），使其成为包含商品名称的独立完整问题。

请直接返回JSON格式结果，格式如下：
{{
    "item_names": ["商品A", "商品B"],
    "rewritten_query": "关于商品A和商品B，..."
}}